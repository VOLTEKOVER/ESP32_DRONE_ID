name: OmniRID Desktop CI

on:
  push:
    branches: [ "main", "universal" ]
    paths:
      - 'OmniRID-Desktop/**'
  pull_request:
    branches: [ "main", "universal" ]
    paths:
      - 'OmniRID-Desktop/**'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    name: OmniRID Desktop (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest]

    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1

      - name: Setup Node.js
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: OmniRID-Desktop/package-lock.json

      - name: Install dependencies
        run: npm ci
        working-directory: OmniRID-Desktop

      - name: Verify build (pack)
        run: npx electron-builder --dir
        working-directory: OmniRID-Desktop
