[package]
name = "proto-mavlink"
version = "0.1.0"
edition = "2021"
description = "MAVLink v1/v2 parser input (port of mavlink_parser.c), implements the rid-interface GpsSource contract"

[dependencies]
rid-interface = { path = "../../firmware/rid-interface" }
libm = "0.2"
